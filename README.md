# tenders-go

**tenders-go** — серверная часть системы управления тендерами с асинхронной RAG-обработкой. Проект реализован на Go, взаимодействует с PostgreSQL (pgvector), интегрируется с Python-воркерами для AI-аналитики и семантического поиска.

**Система обеспечивает:**
- 📥 Автоматический импорт тендеров из XLSX
- 🧠 Умное сопоставление позиций через RAG (Google File Search)
- ⚡ Кэширование с TTL для мгновенной обработки повторяющихся данных
- 🤖 AI-анализ лотов через Google Gemini
- 🔍 Автоматический поиск дубликатов с ручным подтверждением слияния
- 🏗️ Три асинхронных пайплайна: Импорт → Сопоставление → Очистка

---

## Основные возможности

- **Импорт и обработка тендеров**: загрузка XLSX через Python-парсер, автоматическая конвертация в JSON и атомарное сохранение в PostgreSQL. Сохранение "сырого" JSON для аудита и переобработки.
- **Асинхронная RAG-обработка**: трёхуровневая система пайплайнов для сопоставления позиций с каталогом, индексации и поиска дубликатов через Google File Search.
- **Умный кэш сопоставлений**: `matching_cache` с версионированием и TTL для мгновенного сопоставления повторяющихся позиций (Cache Hit/Miss pattern).
- **Трёхуровневая иерархия справочников**: типы тендеров, разделы (chapters), категории с полной поддержкой CRUD.
- **Гибкое управление тендерами и лотами**: создание, частичное обновление (PATCH), удаление с контролем целостности (RESTRICT/CASCADE).
- **AI-аналитика лотов**: интеграция с Google Gemini для извлечения ключевых параметров (`lot_key_parameters`) и классификации работ.
- **Семантический поиск**: pgvector + HNSW-индексы (частичные, по `kind = 'POSITION'`) для быстрого RAG-поиска по каталогу позиций.
- **Human-in-the-loop для дубликатов**: автоматическое обнаружение похожих позиций + ручное подтверждение слияния через `suggested_merges`.
- **API с пагинацией**: все списковые запросы поддерживают постраничную отдачу и фильтрацию.
- **Типизированные ошибки**: разделение валидационных (400) и серверных (500) ошибок через `ValidationError`.
- **Миграции и автогенерация кода**: структура базы и слой доступа к данным на базе [sqlc](https://sqlc.dev/), подробная документация миграций в [`DB_MIGRATIONS_README.md`](cmd/internal/db/migration/DB_MIGRATIONS_README.md).

---

## Структура репозитория

- `cmd/internal/db/query/` — SQL-запросы, используемые с генератором кода [sqlc](https://sqlc.dev/).
- `cmd/internal/db/migration/` — миграции для схемы базы данных (подробное описание в [`DB_MIGRATIONS_README.md`](cmd/internal/db/migration/DB_MIGRATIONS_README.md)).
- `cmd/internal/server/` — обработчики REST API: импорт, справочники, тендеры, предложения, RAG-эндпоинты.
- `cmd/internal/services/` — бизнес-логика и сервисный слой.
- `cmd/internal/api_models/` — структуры данных для обмена через API.
- `cmd/internal/util/` — утилитарный код.
- `cmd/main/` — точка входа, запуск сервера.

---

## 🏗️ Архитектура Системы и Пайплайны

Система построена на **асинхронной архитектуре** с разделением Go-бэкенда и Python-воркеров. Go отвечает за быструю обработку данных и транзакции, Python — за "тяжелую" AI-аналитику и RAG-поиск.

```text
┌─────────────┐
│ XLSX Upload │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  Процесс 1: Импорт и Первичный Анализ              │
│  Python Celery → Go DB → Python Gemini → Go DB     │
│  Результат: Тендер в БД, NULL позиции в очереди    │
└──────┬──────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  Процесс 2: Сопоставление Позиций (каждые 5 мин)  │
│  Go → Python RAG → Google File Search → Go Cache   │
│  Результат: NULL позиции получают catalog_id        │
└──────┬──────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────┐
│  Процесс 3: Очистка Каталога (ночью)               │
│  Go → Python → Google File Search → Go             │
│  Результат: Индекс обновлен, дубликаты найдены     │
└─────────────────────────────────────────────────────┘
```

### 🚀 Процесс 1: Импорт и Первичный Анализ

**Цель:** Мгновенно принять XLSX файл, сохранить его в БД, выполнить "быструю" AI-обработку (анализ лота) и поставить "медленные" задачи (сопоставление позиций) в очередь.

**Триггер:** `POST /api/v1/upload-tender`

**Исполнители:** Python FastAPI + Go-бэкенд

**Ключевые шаги:**
1. **Парсинг (Python):** Celery-воркер парсит XLSX в JSON
2. **Импорт (Go):** `POST /api/v1/import-tender` → транзакция `ImportFullTender`
3. **Сохранение:** `tender`, `lots`, `proposals`, `position_items`, `tender_raw_data`
4. **Cache Check (Go):** Для каждой `position_item`:
   - **Cache Hit:** Хэш найден в `matching_cache` → сразу проставляется `catalog_position_id`
   - **Cache Miss:** Хэш не найден → `catalog_position_id = NULL`, создается запись в `catalog_positions` со `status = 'pending_indexing'`
5. **AI-анализ лота (Python):** Gemini генерирует `ai_data` (параметры лота)
6. **Сохранение AI-данных (Go):** `POST /api/v1/lots/:lot_id/ai-results`

**Результат:** Тендер сохранен, легкая аналитика выполнена, тяжелая — в очереди.

---

### 🧠 Процесс 2: Сопоставление Позиций (RAG)

**Цель:** Найти все "осиротевшие" `position_items` (где `catalog_position_id IS NULL`) и сопоставить их с каталогом через RAG-поиск.

**Триггер:** Каждые 5 минут (Celery Beat)

**Исполнители:** Python RAG-воркер + Go-бэкенд + Google File Search

**Ключевые эндпоинты:**
- `GET /api/v1/positions/unmatched` — получить очередь необработанных позиций
- `POST /api/v1/positions/match` — сопоставить позицию с каталогом

**Ключевые шаги:**
1. **Запрос очереди (Python):** `get_unmatched_positions(limit=100)`
2. **Формирование очереди (Go):** Рекурсивный SQL `GetUnmatchedPositions` с "хлебными крошками" (`rich_context_string`)
3. **RAG-поиск (Python):** Для каждой позиции → Google File Search → находит `catalog_id`
4. **Сопоставление (Go):** Транзакция:
   - `UPDATE position_items SET catalog_position_id = :catalog_id`
   - `UPSERT INTO matching_cache` (TTL 30 дней)

**Результат:** NULL-позиции получают правильный `catalog_position_id`, кэш пополняется.

---

### 🧹 Процесс 3: Очистка и Индексация Каталога

**Цель:** Поддерживать RAG-индекс в актуальном состоянии и находить дубликаты в `catalog_positions`.

**Триггер:** Каждую ночь (Celery Beat)

**Исполнители:** Python RAG-воркер + Go-бэкенд + Google File Search

**Ключевые эндпоинты:**
- `GET /api/v1/catalog/unindexed` — получить позиции для индексации
- `POST /api/v1/catalog/indexed` — подтвердить индексацию
- `POST /api/v1/merges/suggest` — предложить слияние дубликатов

#### Часть А: Индексация
1. **Запрос очереди (Python):** Позиции со `status = 'pending_indexing'`
2. **Создание JSONL (Python):** Генерация `catalog.jsonl`
3. **Загрузка в RAG (Python):** `FileSearchClient.initialize_catalog()`
4. **Подтверждение (Go):** `UPDATE catalog_positions SET status = 'active'`

#### Часть Б: Поиск дубликатов
1. **Само-поиск (Python):** Для каждой позиции → поиск себя в RAG
2. **Обнаружение дубликатов:** Если RAG вернул другой `catalog_id`
3. **Создание задачи (Go):** `UpsertSuggestedMerge` → очередь для оператора

**Результат:** RAG-индекс актуален, очередь слияний наполнена для ручной проверки.

---

## API Эндпоинты

### Основные
- `GET /api/stats` — статистика системы
- `POST /api/v1/import-tender` — импорт тендера из JSON
- `POST /api/v1/upload-tender` — загрузка XLSX (проксирование в Python)
- `GET /api/v1/tasks/:task_id/status` — статус фоновой задачи

### Тендеры и лоты
- `GET /api/v1/tenders` — список тендеров (с пагинацией)
- `GET /api/v1/tenders/:id` — детали тендера
- `PATCH /api/v1/tenders/:id` — частичное обновление тендера
- `GET /api/v1/tenders/:id/proposals` — предложения по тендеру
- `POST /api/v1/lots/:lot_id/ai-results` — сохранение AI-анализа лота
- `GET /api/v1/lots/:id/proposals` — предложения по лоту
- `PATCH /api/v1/lots/:id/key-parameters` — обновление ключевых параметров лота

### Справочники
- `GET/POST/PUT/DELETE /api/v1/tender-types` — типы тендеров
- `GET/POST/PUT/DELETE /api/v1/tender-chapters` — разделы тендеров
- `GET/POST/PUT/DELETE /api/v1/tender-categories` — категории тендеров
- `GET /api/v1/tender-types/:type_id/chapters` — разделы по типу
- `GET /api/v1/tender-chapters/:chapter_id/categories` — категории по разделу

### RAG-воркфлоу
- `GET /api/v1/positions/unmatched` — очередь несопоставленных позиций
- `POST /api/v1/positions/match` — сопоставление позиции с каталогом
- `GET /api/v1/catalog/unindexed` — позиции каталога для индексации
- `POST /api/v1/catalog/indexed` — подтверждение индексации
- `POST /api/v1/merges/suggest` — предложение слияния дубликатов

---

## Примеры последних изменений (2025)

- **RAG-воркфлоу (ноябрь 2025):** Внедрена полная асинхронная обработка позиций через RAG (3 пайплайна), добавлены эндпоинты для взаимодействия с Python-воркерами, реализован кэш сопоставлений с TTL.
- **Миграция 000005:** Добавлен жизненный цикл позиций каталога (`status`: pending_indexing/active/deprecated/archived/na).
- **Миграция 000004:** Реализована асинхронная RAG-обработка каталога: поле `kind` для классификации, таблицы `matching_cache` и `suggested_merges`, частичные HNSW-индексы.
- **Типизированные ошибки:** Внедрен `ValidationError` для разделения ошибок валидации (400) и серверных ошибок (500).
- **Рефакторинг импорт-обработчика тендеров:** Полностью переработан для надёжной обработки JSON, строгой валидации и атомарных транзакций с сохранением "сырого" JSON.
- **Унификация сервисных функций:** Добавлены обобщённые `getOrCreateOrUpdate` для ключевых сущностей, реализован паттерн "Cache Hit/Miss".
- **Разделение API-обработчиков:** Каждый handler вынесен в отдельный файл (`handlers_tender.go`, `handlers_import.go`, `handlers_rag.go`, и др.).
- **PATCH-обновление:** Поддержка частичного обновления тендеров и лотов.
- **Обновлены структуры API:** Предложения содержат `additional_info` как JSON, добавлены rich-context для RAG.

---

## Технологии

- **Go 1.21+** — основная бизнес-логика и REST API
- **PostgreSQL 15+** с расширением `pgvector` — хранение данных и векторный поиск
- **sqlc** — генерация strongly-typed Go кода из SQL
- **Gin** — HTTP-фреймворк для API
- **HNSW-индексы** — быстрый семантический поиск (частичные индексы для `kind = 'POSITION'`)
- **Python FastAPI + Celery** — асинхронные воркеры (парсинг, RAG, AI-анализ)
- **Google Gemini + File Search** — AI-аналитика и RAG-поиск

---

## Ключевые сущности

### Основные таблицы
- **Тендеры** (`tenders`) — основная информация о тендере
- **Лоты** (`lots`) — лоты тендера с AI-параметрами (`lot_key_parameters`)
- **Объекты** (`objects`), **Исполнители** (`executors`)
- **Подрядчики** (`contractors`), **Контактные лица** (`persons`)
- **Предложения** (`proposals`), **Победители** (`winners`)

### RAG-инфраструктура
- **catalog_positions** — "золотой" справочник работ с полями `kind` и `status`
- **position_items** — детализация предложений с `catalog_position_id` (может быть NULL)
- **matching_cache** — кэш сопоставлений с TTL для ускорения импорта
- **suggested_merges** — очередь задач по слиянию дубликатов (human-in-the-loop)

### Документация и индексы
- **lots_md_documents** — Markdown-документация лотов
- **lots_chunks** — чанки для RAG по документам
- **tender_raw_data** — исходный JSON для аудита и переобработки

---

## Принципы работы с удалением

- `ON DELETE RESTRICT` — предотвращает удаление записей с внешними связями.
- `ON DELETE CASCADE` — каскадное удаление связанных данных.
- Контроль целостности через БД и уровень приложения.

---

## 🚀 Быстрый старт

### Требования
- Go 1.21+
- Docker (для PostgreSQL с pgvector)
- Make

### Запуск

```bash
# 1. Клонировать репозиторий
git clone https://github.com/zhukovvlad/tenders-go.git
cd tenders-go

# 2. Настроить PostgreSQL (запустит контейнер, создаст БД и применит миграции)
make setup-db

# 3. Запустить сервер
make run

# Сервер запустится на http://127.0.0.1:8080
```

### Полезные команды

```bash
make run              # Запуск Go-сервера
make sqlc             # Генерация кода из SQL
make migrateup        # Применить миграции
make migratedown      # Откатить последнюю миграцию
make docker-start     # Запустить существующий контейнер PostgreSQL
make docker-stop      # Остановить контейнер PostgreSQL
```

### Интеграция с Python-воркерами

Для полной функциональности RAG-пайплайнов необходимо запустить Python-часть системы:
- **Парсер XLSX** (FastAPI + Celery)
- **RAG-воркер** (сопоставление позиций и индексация каталога)
- **AI-воркер** (Gemini для анализа лотов)

*Примечание: Python-репозиторий находится в разработке и будет опубликован отдельно.*

---

## TODO

Список актуальных задач по доработке SQL-запросов и бизнес-логики: [`cmd/internal/db/query/TODO.md`](cmd/internal/db/query/TODO.md)

---

## Контрибьюция

PR и багрепорты приветствуются! Для сложных изменений и архитектурных вопросов — пожалуйста, создавайте issue для обсуждения.

---

**Автор:** [zhukovvlad](https://github.com/zhukovvlad)
