# Консолидированная Миграция БД v1.1

## 📋 Общая информация

Это **единая миграция**, объединяющая функциональность семи последовательных миграций (v1-v7) в один атомарный SQL-скрипт, плюс дополнительные миграции для новой функциональности.

**Дата создания:** 5 февраля 2026  
**Версия базы:** 000001_init_schema_consolidated → **000002_add_fts_to_catalog**  
**Статус:** ✅ Производственная (Production-ready)

---

## 🎯 Цель объединения

### Преимущества консолидированной миграции:

1. **Быстрое развертывание** — один файл вместо 7 последовательных шагов
2. **Атомарность** — вся схема создается в одной транзакции
3. **Упрощенная отладка** — легче найти и исправить ошибки
4. **Чистая история** — новые окружения не "несут багаж" инкрементальных изменений
5. **Актуальная документация** — вся архитектура в одном месте

### Когда использовать:

✅ Новые окружения (dev, staging, production с нуля)  
✅ CI/CD pipelines  
✅ Локальная разработка (чистая БД)  
✅ Тестовые окружения

❌ **НЕ использовать** на production с существующими данными

### ⚠️ Почему нельзя на production с данными?

**Проблема версионирования:**
```text
Production БД:           Консолидированная миграция:
schema_migrations        schema_migrations
├─ version: 7           ├─ version: 1  ❌ КОНФЛИКТ!
└─ dirty: false         └─ dirty: false
```

**Что произойдет:**
1. `migrate` увидит, что версия 1 еще не применена (хотя схема идентична v7)
2. Попытается выполнить `CREATE TABLE` для существующих таблиц
3. Получит ошибку: `ERROR: relation "catalog_positions" already exists`
4. База останется в состоянии `dirty: true`

**Правильные сценарии для production:**

**Вариант A:** Оставить старые миграции (ничего не делать)
```bash
# База уже на v7 → продолжаем использовать инкрементальные миграции
# Файлы в /archive_v1-v7/ остаются как резервная копия
```

**Вариант B:** Полное пересоздание (ТОЛЬКО если данные не важны!)
```bash
# 1. Бэкап данных (опционально)
pg_dump tendersdb > backup.sql

# 2. Удалить БД и создать заново
make dropdb && make createdb && make migrateup

# 3. Восстановить данные (если нужно)
psql tendersdb < backup.sql
```

**Вариант C:** Ручная синхронизация версий (для экспертов)
```sql
-- ОПАСНО! Только если вы уверены, что схемы идентичны
TRUNCATE schema_migrations;
INSERT INTO schema_migrations (version, dirty) VALUES (1, false);
```

---

## 🏗️ Архитектура базы данных

### Структура по разделам:

#### **1. Расширения**
- `pgvector` — векторный поиск для RAG

#### **2. Справочники** (4 таблицы)
```text
units_of_measurement     — Единицы измерения (м², м³, шт и т.д.)
tender_types             — Типы тендеров
  ├─ tender_chapters     — Главы (разделы работ)
      └─ tender_categories  — Категории работ
```

#### **3. RAG-инфраструктура** (3 таблицы + 1 VIEW)
```text
catalog_positions        — Каталог работ (core)
  ├─ fts_vector         — GENERATED COLUMN для полнотекстового поиска (tsvector)
  ├─ matching_cache      — Кэш семантического поиска (TTL)
  └─ suggested_merges    — Очередь задач для дедупликации (Human-in-the-Loop)

catalog_positions_clean  — VIEW: только валидные позиции (kind='POSITION')
```

**Ключевые особенности:**
- **Классификация** через поле `kind`: `POSITION`, `HEADER`, `TRASH`, `TO_REVIEW`
- **Жизненный цикл** через поле `status`: `pending_indexing`, `active`, `deprecated`, `archived`, `na`
- **Уникальность** по паре: `(standard_job_title, unit_id)` — одна работа = одна единица измерения
- **Частичный HNSW-индекс** только для `kind='POSITION'` (ускоряет RAG на 3-5x)
- **FTS (Full-Text Search)** для гибридного поиска RRF (Reciprocal Rank Fusion): векторный + лексический

#### **4. Тендеры и лоты** (6 таблиц)
```text
objects                  — Объекты строительства
executors                — Организаторы тендеров
tenders                  — Тендеры
  ├─ tender_raw_data     — Source of Truth (JSONB)
  └─ lots                — Лоты
      ├─ lots_md_documents    — Документация (Markdown)
      └─ lots_chunks          — Чанки для RAG (vector embeddings)
```

**Важно:**
- При удалении тендера **НЕ удаляются** лоты (FK без CASCADE)
- При удалении лота **каскадно удаляются** документы и предложения

#### **5. Предложения подрядчиков** (8 таблиц)
```text
contractors              — Подрядчики
  └─ persons             — Контактные лица
proposals                — Предложения (сметы)
  ├─ winners             — Победители
  ├─ proposal_additional_info   — Доп. информация
  ├─ proposal_summary_lines     — Сводные строки
  └─ position_items      — Детальные позиции сметы (строки)
```

**Каскадное удаление:** При удалении `proposals` **автоматически удаляются** все дочерние записи (`winners`, `position_items` и т.д.)

#### **6. Авторизация** (2 таблицы)
```text
users                    — Локальные пользователи (bcrypt)
  └─ user_sessions       — Refresh-токены (SHA-256 hash)
```

**RBAC:** `admin`, `operator`, `viewer`

---

## 📊 Статистика

| Показатель | Значение |
|------------|----------|
| Таблиц | 24 |
| VIEW | 1 |
| GENERATED COLUMN | 1 |
| Индексов | ~37+ |
| Внешних ключей | 22 |
| CHECK-ограничений | 8 |
| HNSW-индексов | 3 |
| GIN-индексов | 2 |

---

## 🚀 Применение миграции

### Новое окружение (с нуля):

```bash
# 1. Создать пустую БД
make createdb

# 2. Применить все миграции (000001 → 000002)
make migrateup

# 3. Сгенерировать Go-код
make sqlc

# 4. Создать админа (опционально)
make createadmin
```

**Примечание:** `make migrateup` автоматически применит все доступные миграции в правильном порядке:
1. `000001_init_schema_consolidated` — базовая структура (v1-v7)
2. `000002_add_fts_to_catalog` — полнотекстовый поиск (v8)

### Пересоздание существующей БД:

```bash
# ВНИМАНИЕ: Все данные будут удалены!
make dropdb && make createdb && make migrateup && make sqlc
```

---

## 🔄 Откат миграции

```bash
# Откатить последнюю миграцию (000002 → 000001)
make migratedown

# Откатить все миграции (до версии 0)
migrate -path cmd/internal/db/migration -database "$DATABASE_URL" down
```

**⚠️ Осторожно:** Откат удаляет ВСЕ данные без возможности восстановления!

**Порядок отката:**
1. `000002_add_fts_to_catalog.down.sql` — удаляет FTS-индекс и колонку
2. `000001_init_schema_consolidated.down.sql` — удаляет все таблицы и VIEW

---

## 🗂️ Восстановление старых миграций

Если нужно вернуться к инкрементальным миграциям:

```bash
# 1. Удалить консолидированную версию
rm cmd/internal/db/migration/000001_init_schema_consolidated.*

# 2. Восстановить из архива
cp cmd/internal/db/migration/archive_v1-v7/00000[1-7]_*.sql \
   cmd/internal/db/migration/

# 3. Пересоздать БД
make dropdb && make createdb && make migrateup
```

📁 **Архив:** `/cmd/internal/db/migration/archive_v1-v7/`

---

## ⚙️ Технические детали

### Расширения PostgreSQL:
- **pgvector** — для хранения embeddings (размерность 768)

### Генерируемые колонки (GENERATED COLUMN):

```sql
-- catalog_positions.fts_vector (v8)
-- Автоматически создаётся из standard_job_title для полнотекстового поиска
"fts_vector" tsvector 
GENERATED ALWAYS AS (
  to_tsvector('simple', coalesce("standard_job_title", ''))
) STORED
```

**Особенности:**
- Конфигурация **'simple'** (без морфологии), так как данные уже лемматизированы через spaCy
- Автоматическое обновление при изменении `standard_job_title`
- STORED на диске для максимальной скорости поиска

### Специальные индексы:

#### HNSW (векторный поиск):
```sql
-- Частичный индекс только для позиций каталога
idx_cp_kind_pos_hnsw WHERE kind = 'POSITION'

-- Полные индексы для документации лотов
lots_chunks_embedding_idx
```

#### GIN (JSONB + FTS):
```sql
idx_gin_lots_key_parameters      -- Быстрый поиск по техпараметрам
idx_catalog_positions_fts        -- Полнотекстовый поиск (FTS) для RRF (v8)
```

#### Partial (условные):
```sql
idx_cp_kind_review WHERE kind = 'TO_REVIEW'
idx_cp_status_pending WHERE status = 'pending_indexing'
idx_suggested_merges_status_created WHERE status = 'PENDING'
idx_user_sessions_user_active WHERE revoked_at IS NULL
```

### Уникальные ограничения:

```sql
-- Каталог: название + единица измерения
uq_catalog_positions_title_unit (standard_job_title, COALESCE(unit_id, -1))

-- Предложение: один подрядчик = одна смета на лот
uq_proposals_lot_contractor (lot_id, contractor_id)

-- Лот: уникальный ключ в рамках тендера
lots_tender_id_lot_key_idx (tender_id, lot_key)
```

---

## 📝 История миграций

### Консолидированная миграция 000001 (объединяет v1-v7):

| Версия | Описание |
|--------|----------|
| v1 | Базовая структура (тендеры, лоты, каталог) |
| v2 | Правила ON DELETE + векторные индексы |
| v3 | Таблица сырых данных (`tender_raw_data`) |
| v4 | RAG-workflow (кэш, suggested_merges, kind/status) |
| v5 | Статусная модель каталога |
| v6 | Авторизация (users, sessions) |
| v7 | Единицы измерения в каталоге |

### Инкрементальные миграции (после консолидации):

| Версия | Файл | Описание |
|--------|------|----------|
| **v8** | `000002_add_fts_to_catalog` | Полнотекстовый поиск (FTS) для гибридного RAG<br/>• GENERATED COLUMN `fts_vector` (tsvector)<br/>• GIN-индекс для мгновенного поиска<br/>• Конфигурация 'simple' для лемматизированных данных |

---

## 🔍 Важные особенности схемы

### 1. RAG-оптимизация
- **Частичный HNSW** вместо полного (меньше, быстрее)
- **Кэш матчинга** с TTL и версионированием
- **Human-in-the-Loop** для дедупликации
- **Гибридный поиск (RRF)** — комбинация векторного (HNSW) и лексического (FTS) поиска

### 2. Гибкая типизация
- `catalog_positions.kind` — классификация строк
- `catalog_positions.status` — жизненный цикл
- `position_items.catalog_position_id` — **NULLABLE** (async обработка)

### 3. Защита данных
- Email-нормализация: `CHECK (email = LOWER(BTRIM(email)))`
- Hex-валидация токенов: `CHECK (refresh_token_hash ~ '^[0-9a-f]{64}$')`
- Enum-значения через CHECK-ограничения

### 4. Cascade-стратегия
- ✅ Справочники (types → chapters → categories)
- ✅ Лоты → документы → чанки
- ✅ Предложения → детализация (winners, position_items и т.д.)
- ❌ Тендеры → лоты (защита от случайного удаления)

---

## 🐛 Известные особенности

1. **NULL в catalog_position_id** — это норма (асинхронная обработка Python-воркером)
2. **COALESCE в уникальном индексе** — чтобы `NULL != NULL` в бизнес-логике
3. **Refresh-токены** хранятся только как hash (безопасность)
4. **FTS использует конфигурацию 'simple'** — данные уже лемматизированы через spaCy, поэтому не нужна русская морфология PostgreSQL

---

## 🧹 Операционное обслуживание (Housekeeping)

### Очистка кэша с истекшим TTL

Запускать периодически (например, через cron раз в сутки):

```sql
-- Удаляет записи кэша с истекшим сроком жизни
DELETE FROM "matching_cache"
WHERE "expires_at" IS NOT NULL AND "expires_at" < NOW();
```

### Мониторинг RAG-индексации

```sql
-- Статистика по статусам позиций каталога
SELECT 
    status,
    kind,
    COUNT(*) as count
FROM "catalog_positions"
GROUP BY status, kind
ORDER BY status, kind;

-- Поиск "застрявших" позиций (pending > 24 часов)
SELECT 
    id,
    standard_job_title,
    kind,
    created_at,
    NOW() - created_at as pending_duration
FROM "catalog_positions"
WHERE status = 'pending_indexing' 
  AND kind = 'POSITION'
  AND created_at < NOW() - INTERVAL '24 hours'
ORDER BY created_at
LIMIT 100;

-- Проверка корректности связки kind/status (не должно быть результатов)
SELECT 
    id,
    standard_job_title,
    kind,
    status
FROM "catalog_positions"
WHERE 
    (kind = 'POSITION' AND status = 'na')
    OR
    (kind != 'POSITION' AND status IN ('pending_indexing', 'active'))
LIMIT 100;
```

### Очистка отозванных сессий

```sql
-- Удалить старые отозванные сессии (старше 30 дней)
DELETE FROM "user_sessions"
WHERE "revoked_at" IS NOT NULL 
  AND "revoked_at" < NOW() - INTERVAL '30 days';

-- Удалить истекшие сессии
DELETE FROM "user_sessions"
WHERE "expires_at" < NOW();
```

---

## 📞 Контакты и поддержка

- **Репозиторий:** `/cmd/internal/db/migration/`
- **Архив старых миграций:** `/archive_v1-v7/`
- **Документация API:** `/cmd/internal/db/query/Readme.md`

---

## 📄 Лицензия

Внутренний проект. Все права защищены.

---

**Последнее обновление:** 8 февраля 2026  
**Версия схемы:** 2 (000002_add_fts_to_catalog)
